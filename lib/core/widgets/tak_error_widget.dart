import 'package:flutter/material.dart';

class TakErrorWidget extends StatelessWidget {
  const TakErrorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Error");
  }
}
